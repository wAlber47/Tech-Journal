- name: Create OU
  hosts: 10.0.5.5
  gather_facts: no
  tasks: 
  - win_shell: install-packageprovider -name nuget -force
  - name: Install Xactive directory
    win_psmodule:
       name: ActiveDirectoryDSC
       state: present
  - name: Create OU
    win_dsc:
      resource_name: ADOrganizationalUnit
      name: "{{ item.name }}"
      path: "{{ item.path }}"
    loop:
      - { name: 'blue4', path: 'DC=blue4,DC=local' }
      - { name: 'Accounts', path: 'OU=blue4,DC=blue4,DC=local' }
      - { name: 'Groups', path: 'OU=Accounts,OU=blue4,DC=blue4,DC=local' }
      - { name: 'Computers', path: 'OU=blue4,DC=blue4,DC=local' }
      - { name: 'Servers', path: 'OU=Computers,OU=blue4,DC=blue4,DC=local' }
      - { name: 'Workstations', path: 'OU=Computers,OU=blue4,DC=blue4,DC=local' }